# Incremental Implementation Plan: Fibonacci and Factorial

## Overview
Add fibonacci and factorial utility functions to `src/index.js` with proper exports.

todos:
    - id: 1
      content: Implement fibonacci in src/index.js - create a function that computes the nth fibonacci number with input validation for non-negative integers, and export it via module.exports
    - id: 2
      content: Implement factorial in src/index.js - create a function that computes n! with input validation for non-negative integers, and update module.exports to include both fibonacci and factorial
