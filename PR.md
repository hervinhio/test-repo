Add fibonacci and factorial functions

Implement an iterative fibonacci function in src/index.js that computes
the nth fibonacci number for any non-negative integer input.

Implement an iterative factorial function in src/index.js that computes
the factorial of a non-negative integer n (n!) using a loop.
