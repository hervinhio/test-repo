Implement fibonacci and factorial functions

Add an iterative fibonacci function to src/index.js that computes the nth
Fibonacci number for non-negative integers. The function throws an error for
negative inputs and exports via module.exports.

Add an iterative factorial function to src/index.js that computes n! for
non-negative integers. The function throws an error for negative inputs and
matches the existing fibonacci export pattern.
